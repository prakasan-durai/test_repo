version: 2.1
jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run: "echo Hello Earth"
      - run: "echo Goodbye Earth"

workflows:
  say-hello-workflow: 
    jobs:
      - say-hello